> **ARCHIVAL NOTICE:** This document is preserved for historical context. The final, unified proof and analysis are now complete. For the authoritative summary, see `README.md` and `docs/final_fact_check.md`.

# Independent Assessment of Repository Claims

## Methodology
- Installed the Coq toolchain and successfully type-checked the new kernel and sandboxes to confirm that the mechanical subsumption witness and the microcosms compile without axioms (`coqc -Q kernel Kernel kernel/Subsumption.v`, `coqc sandboxes/ToyThieleMachine.v`, `coqc sandboxes/VerifiedGraphSolver.v`).【fbb5c4†L1-L1】【ff49c2†L1-L1】【c76533†L1-L1】【fb377a†L1-L1】
- Parsed the published Yosys JSON netlists to recover the reported resource counts and attempted to replay the synthesis harness to verify tool availability.【2afd1d†L1-L15】【fdfb8b†L1-L6】
- Reviewed the graph-colouring experiment outputs and inspected the synthesis-trap staging area to check which evidence is actually present in the tree.【61e8a9†L1-L46】【da4017†L1-L2】
- Ran the maintained subsumption build to evaluate the status of the large-scale containment proof.【2f2c9f†L1-L110】

## Findings

### 1. Mechanical subsumption and microcosms now exist
The kernel development introduces a shared tape/head state with a Turing interpreter that ignores `H_ClaimTapeIsZero` and a Thiele interpreter that executes it by zeroing the tape and incrementing `mu_cost`. `thiele_simulates_turing` and `turing_is_strictly_contained` prove that the Thiele kernel reproduces every classical execution and reaches a state the Turing kernel cannot.【F:coq/kernel/Kernel.v†L4-L66】【F:coq/kernel/KernelTM.v†L12-L37】【F:coq/kernel/KernelThiele.v†L7-L26】【F:coq/kernel/Subsumption.v†L36-L118】 In parallel, `ToyThieleMachine.v` and `VerifiedGraphSolver.v` continue to offer microcosms showing the same phenomenon on finite examples, with the latter recording 1,288 description bits plus nine `(3 → 1)` information ratios under μ-spec v2.0 to finish with zero residual search.【F:coq/sandboxes/ToyThieleMachine.v†L1-L94】【F:coq/sandboxes/VerifiedGraphSolver.v†L9-L177】 The subsumption result is definitional—it relies on granting the Thiele kernel an extra primitive—but it is now mechanically established.

`coq/kernel/ThieleCPUBridge.v` connects this minimalist model to the real `thielecpu` interpreter by abstracting the VM instruction set, compiling each opcode to an `H_ClaimTapeIsZero` payload, and proving that the compiled kernel run matches the VM execution whenever the program stays within the audited error-free receipts.【F:coq/kernel/ThieleCPUBridge.v†L8-L255】 This closes the formal gap between the Coq kernel and the Python implementation: for the μ-ledger and program counter components, the VM is now a verified instance of the kernel interpreter.

### 2. Empirical receipts now bridge into Coq
The new `scripts/translate_receipts_to_coq.py` utility converts Act III receipts into `coq/sandboxes/GeneratedProof.v`, allowing `scripts/prove_it_all.sh` to replay a fresh VM execution inside Coq after compiling `Sandbox.VerifiedGraphSolver`. This closes the earlier gap between the Python pipeline and the formal developments: the same witness recorded in JSON is now mechanically checked against the microcosm definitions.【F:scripts/translate_receipts_to_coq.py†L1-L187】【F:scripts/prove_it_all.sh†L1-L26】【F:coq/sandboxes/GeneratedProof.v†L1-L120】 Auditors can therefore treat the triadic cascade run as a single interlocking artifact spanning empirical and formal domains.

### 3. Empirical evidence shows constant-factor, not exponential, gains
The refreshed graph-colouring experiment still records 3,786 blind candidates for Act I, 18 heuristic branches for Act II, and zero brute-force checks for Act III after spending ≈1302.26 μ-bits (1,288 description bits plus nine `log₂ 3` gains) on oracle queries for the triadic cascade under μ-spec v2.0.【F:graph_demo_output/triadic_cascade/analysis_report.json†L13-L40】【F:spec/mu_spec_v2.md†L1-L74】 The new scaling suite extends this measurement to additional cascade graphs (5, 7, and 10 nodes), emitting `scaling_summary.json` and `scaling_plot.png` that show Act III holding at zero candidate checks while Act II grows with the instance size.【F:graph_demo_output/scaling_summary.json†L1-L24】【F:graph_demo_output/scaling_plot.png†L1-L1】 The evidence therefore supports consistent constant-factor collapses, not an asymptotic separation.

### 4. Core containment proof remains unverified
The historical subsumption development has been archived under `archive/research/incomplete_subsumption_proof/`, and `coq/verify_subsumption.sh` now reports the archival status instead of attempting to build it. The gap persists—the flagship containment argument is still incomplete—but the repository no longer presents the failing script as an active proof obligation.【F:coq/verify_subsumption.sh†L1-L40】【F:archive/research/incomplete_subsumption_proof/README.md†L1-L60】

### 5. Synthesis trap evidence is now reproducible or inspectable
The repository carries Verilog sources, JSON netlists, and the captured Yosys logs for the classical solver and the new Thiele graph solver (228 cells vs. 866 cells; 267 vs. 1,237 wire bits, with 517 cells assigned to the combinational `reasoning_core`).【F:hardware/synthesis_trap/classical_solver.log†L780-L806】【F:hardware/synthesis_trap/thiele_graph_solver.log†L1820-L1884】 `scripts/run_the_synthesis.sh` now checks for `yosys` at startup: if the tool is present, it rebuilds the designs; otherwise, it streams the archived logs, ensuring the oracle's verdict is visible even without re-running synthesis.【F:scripts/run_the_synthesis.sh†L1-L53】 The hardware μ-ledger now matches μ-spec v2.0: the reasoning fabric reports the same 1,288 question bits and 1302.2646 μ-bits seen in the Python receipts, while still exposing the 23-count legacy activity counter for comparison.【F:hardware/synthesis_trap/reasoning_core.v†L1-L132】【F:hardware/synthesis_trap/thiele_graph_solver.v†L1-L192】 The new autonomous controller (`thiele_autonomous_solver.v`) performs speculative search and backtracking entirely in hardware, and the updated dual testbench shows that both controllers converge on the triadic cascade colouring with identical μ-ledger totals and zero backtracks for the autonomous run.【F:hardware/synthesis_trap/thiele_autonomous_solver.v†L1-L388】【F:hardware/synthesis_trap/thiele_graph_solver_tb.v†L1-L182】

## Conclusion
The repository has undeniably advanced beyond the original paper by supplying mechanically checked microcosms and a richer empirical demo, but several statements in the recent assessment overreach. The large-scale subsumption proof remains broken, the certificate story does not yet cover the real VM receipts, the empirical evidence addresses only a fixed small graph, and the synthesis trap requires additional setup before it can act as an independent witness. Auditors should treat the new artifacts as promising progress rather than definitive proof of the broader thesis.
