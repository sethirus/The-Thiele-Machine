# Minimal Python dependencies needed to run the repository's core tooling and tests.
# This repo is installed with `pip install -e . --no-deps`; requirements.txt only
# supplies the environment packages not provided by the repo itself.

cryptography==46.0.7
hypothesis==6.98.0
pytest==9.0.3
z3-solver==4.15.1.0
