[package]
name = "thiele-zk-guest"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
hex = "0.4"

# Note: In production, use risc0-zkvm or sp1-zkvm
# This is a placeholder for the ZK guest program structure
