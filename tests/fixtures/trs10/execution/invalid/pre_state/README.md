# Execution Invalid Fixtures: Pre-State Mismatch

This directory contains a validly signed execution receipt whose claimed pre-state digest was altered after receipt generation so replay verification fails before accepting the execution facts.
