# Execution Invalid Fixtures: Unsupported Nondeterminism

This directory contains a validly signed execution receipt that declares an unsupported execution state model. Verifiers must reject it as out of scope rather than treating it as replay-verifiable.
