# Execution Invalid Fixtures: Mu Mismatch

This directory contains a validly signed execution receipt whose claimed `mu` movement was altered after receipt generation so replay verification fails on `mu` facts.
