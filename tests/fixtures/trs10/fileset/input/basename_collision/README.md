# Fileset Input Fixtures: Basename Collision

This directory contains two distinct files with the same basename in different subdirectories. Receipt creation must reject them because TRS-1.0 stores file paths by basename only.
