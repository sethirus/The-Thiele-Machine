# Fileset Invalid Fixtures: Metadata Mutation

This directory contains a `fileset` receipt whose `metadata` object was mutated after signing so verification fails because metadata is part of the signed payload.
