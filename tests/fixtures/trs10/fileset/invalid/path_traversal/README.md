# Fileset Invalid Fixtures: Path Traversal

This directory contains a signed `fileset` receipt whose file entry uses an unsupported traversal-style path so verifiers must reject it before file lookup.
