# Fileset Invalid Fixtures: JSON Edge Case

This directory contains a signed `fileset` receipt that uses the wrong JSON scalar type for a required field so verifiers reject numeric and string ambiguity at the schema boundary.
