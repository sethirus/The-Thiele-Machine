# Hello World Program for Thiele Kernel
# This is a placeholder - actual bytecode would be needed for real execution

# The program would emit "hello" as bytes
# Example conceptual bytecode:
# EMIT 'h', EMIT 'e', EMIT 'l', EMIT 'l', EMIT 'o', HALT

# To run (once kernel supports program loading):
# python3 thiele_min.py --run examples/hello.thiele
