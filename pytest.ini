[pytest]
testpaths = tests
addopts = -ra
markers =
    integration: marks tests that exercise external toolchains (iverilog/vvp) to align RTL with VM decoding
    timeout: optional per-test timeout marker (used when pytest-timeout is installed)
